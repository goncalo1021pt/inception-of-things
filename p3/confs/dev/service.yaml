apiVersion: v1
kind: Service
metadata:
  name: playground
  labels:
    app.kubernetes.io/name: playground
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: playground
  ports:
    - name: http
      port: 8888
      targetPort: http
      nodePort: 30080
      protocol: TCP
